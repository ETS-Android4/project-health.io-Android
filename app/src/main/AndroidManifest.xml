<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.srvraj311.smart_health_management">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHealthManagement"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".LoginSignup.ResetPassword.EnterOTP"></activity>
        <activity android:name=".LoginSignup.ResetPassword.ChangePwd" />
        <activity
            android:name=".HomePage.ui.HomeScreen"
            android:label="@string/title_activity_home_screen"
            android:theme="@style/Theme.SmartHealthManagement.NoActionBar" /> <!-- Uses android:usesCleartextTraffic="true" Should be disabled in final Build -->
        <activity
            android:name=".LoginSignup.LoginActivity"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity
            android:name=".LoginSignup.SignupActivity"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity android:name=".LoginSignup.WelcomeScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>